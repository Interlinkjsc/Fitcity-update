<style>
  :root {
    --fc-green: #16A34A;
    --fc-green-soft: #22C55E;
    --fc-orange: #F97316;
    --fc-orange-soft: #FB923C;
    --fc-bg-page: #F8FAF7;
    --fc-bg-surface: #FFFFFF;
    --fc-border: #E5E7EB;
    --fc-text-primary: #0F172A;
    --fc-text-secondary: #475569;
    --fc-text-muted: #64748B;
    --fc-danger: #DC2626;
  }

  .text-primary { color: var(--fc-green); }
  .bg-primary { background-color: var(--fc-green); }
  .text-accent { color: var(--fc-orange); }
  .bg-accent { background-color: var(--fc-orange); }
  .bg-page { background-color: var(--fc-bg-page); }
  .bg-surface { background-color: var(--fc-bg-surface); }
  .border-soft { border-color: var(--fc-border); }
  .text-main { color: var(--fc-text-primary); }
  .text-sub { color: var(--fc-text-secondary); }

  /* Custom Scrollbar for premium feel */
  .custom-scrollbar::-webkit-scrollbar {
    width: 6px;
  }
  .custom-scrollbar::-webkit-scrollbar-track {
    background: transparent;
  }
  .custom-scrollbar::-webkit-scrollbar-thumb {
    background: #cbd5e1;
    border-radius: 10px;
  }
  .custom-scrollbar::-webkit-scrollbar-thumb:hover {
    background: #94a3b8;
  }
</style>
