# Bug report 23/7/2026 — FitCity ERP + Website

Nguồn: `Bug23:7.xlsx`. Repo ERP = thư mục hiện tại (Express + EJS + Mongo). Repo web = `../fitcity-web` (Astro).
Branch deploy: `fix/bug-report-2jul`. Production: erp.fitcity.fit (ERP :3000), fitcity.fit (web :4000).

## TK_ADMIN
- A1 (r3) Hồ sơ nhân sự: tk **Manager** thiếu form set KPI doanh thu cá nhân (role khác đã có).
- A2 (r4) Kỷ luật: khi **sửa** vi phạm bị bắt gõ lại tên nhân sự (không giữ nhân sự đã chọn).
- A3 (r5) Zalo OA: chưa gửi ZNS sau check in/out. (Blocker: token OA — chờ App ID/Secret user).
- A4 (r6) Sổ chi nội bộ: **bỏ tính năng upload file** chứng từ (không cần Drive nữa).
- A5 (r7) Kho nội dung: **bỏ tính năng upload file** tài nguyên.
- A6 (r8) Phân quyền: tk **MKT** vẫn view/tạo được nhân sự dù không có quyền → phải chặn.
- A7 (r9) Tạo hợp đồng: chi nhánh mặc định = PARK CITY → **bỏ default chi nhánh** ở MỌI tk.
- A8 (r10) Daily report: thiếu view chi tiết báo cáo TRƯỚC & SAU khi duyệt.
- A9 (r11) Dinh dưỡng chờ duyệt: UI client tràn dài trên mobile, không kéo ngang được, thiếu nút sửa chi tiết.
- A10 (r12) Quản lý chi nhánh: ID manager (hash mã hoá) tràn dài dưới tên → ẩn.
- A11 (r13) Dashboard "hợp đồng chờ thanh toán": click "duyệt thu ngay" phải nhảy vào chi tiết HĐ để chọn thanh toán; HĐ đã trả đủ phải rớt khỏi danh sách.
- A12 (r14) Phần thưởng: sửa lại UI chi tiết phần thưởng.
- A13 (r15) Tạo KH mới điền đủ nhưng **giới tính không hiện trong hợp đồng** (preview: "Giới tính: ......", radio Nam/Nữ không tick).
- A14 (r16) Mã hợp đồng: theo format FitCity `ngay.thang.nam/tên viết tắt client` (vd 12.07.2026/NVA), cho khách tự điền/tuỳ chỉnh.
- A15 (r17) Lịch dạy PT: bảng "danh sách lịch dạy" (admin) giờ tập/kết thúc **lệch ~7h** so với lịch của tk PT (list 13:05 vs popup 20:10 = lệch múi giờ UTC+7).
- A16 (r18) Quản lý KH: thêm tool **upload & download** danh sách KH (excel/csv) theo template.
- A17 (r19) Lương: **công thức comm dạy PT sai** — phải = %(giá/buổi tập) cộng dồn theo buổi. VD 10% của 500k/buổi = 50k/buổi. HĐ chưa thanh toán / đặt cọc VẪN tính comm dạy.
- A18 (r20) Lương: mô tả chu kỳ trả lương (15 hàng tháng trả ngày 1-15; ngày 5 trả 16-cuối tháng trước) — để kế toán tham chiếu.
- A19 (r21) HĐ: đổi **BẢO LƯU → GIA HẠN** hợp đồng, max 6 tháng, 200k/tháng, sau 6 tháng tự huỷ không hoàn tiền. Ghi phí + thời gian gia hạn vào lịch sử thanh toán.

## TK_MANAGER
- M1 (r22) Tạo HĐ: chi nhánh mặc định PARK CITY → bỏ default (trùng A7).
- M2 (r23) Dinh dưỡng chờ duyệt UI tràn (trùng A9).
- M3 (r24) Tạo HĐ / tạo hội viên mới: thiếu input địa chỉ, ngày sinh, cccd, ngày cấp, nơi cấp cccd, liên lạc khẩn cấp/tên.

## TK_MKT
- MK1 (r25) MKT không có quyền nhân sự sao vẫn tạo được nhân sự (trùng A6).

## TK_PT
- P1 (r26) Tạo HĐ default chi nhánh PARK CITY (trùng A7).
- P2 (r27) Dinh dưỡng "lên phác đồ mới": UI chỉ số/thời gian bị cắt số, bỏ default bữa ăn để PT tự set, nút "+ thêm bữa" không hoạt động.
- P3 (r28) Không lên được lịch dạy sau khi HĐ mới ĐÃ nhận cọc (hiện yêu cầu 100%) → cho lên lịch khi đã có cọc/Deposit.
- P4 (r29) QR scan check in/out chậm → tối ưu (mã QR quá dài nhiều trường info).

## TK_CLIENT
- C1 (r30) Hồ sơ cá nhân: thiếu các trường theo form HĐ.
- C2 (r31) Hợp đồng: client view thiếu thông tin đã điền (admin view đủ).

## WEBSITE
- W1 (r32) Chương trình GymKid/BoxingKid/PilatesKid không load ảnh (admin đã gắn link).
- W2 (r33) Ảnh chi nhánh chưa hiện ở trang index (đã đẩy đủ ảnh).
- W3 (r34) Slot "branch-<slug>-hero": up ảnh trong Cài đặt Website xong không hiện ra web (mọi trang chi nhánh, ảnh vỡ).
